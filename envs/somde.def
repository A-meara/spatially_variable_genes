Bootstrap: localimage
From: python_base_ob.sif

%post
    # Install Python packages for SOMDE
    # Use --no-build-isolation so somoclu can see numpy during build
    pip install --no-cache-dir --break-system-packages --no-build-isolation \
        somde \
        scanpy \
        pandas \
        scipy

    # Clean up
    apt-get clean
    rm -rf /var/lib/apt/lists/*

%labels
    Author OpenProblems/Aidan Meara
    Version 1.0.0

%help
    SOMDE method for spatially variable gene detection
    Based on openproblems/base_python:1.0.0
